interface Company {
    crmUuid: string,
    name: string | null,
    organizationNumber: string | null,
    address: string | null,
    zipCode: string | null,
    city: string | null,
    country: string | null,
    phoneNumber: string | null,
    placeOfStationing: string | null,
    added: string | null,
    updated: string | null,
}